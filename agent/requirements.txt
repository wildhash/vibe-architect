# Vibe Architect — Python Agent Dependencies

# LiveKit agents framework + Google plugin
livekit-agents[google]>=0.12.0
livekit-plugins-google>=0.5.0

# Google Generative AI SDK (direct API mode)
google-generativeai>=0.8.0

# Google Cloud SDKs (Vertex AI + Firestore)
google-cloud-aiplatform>=1.70.0
google-cloud-firestore>=2.19.0

# Pydantic for typed data models
pydantic>=2.7.0

# Environment variable loading
python-dotenv>=1.0.0

# Image processing for video frame capture
Pillow>=10.3.0

# Async HTTP (optional webhook delivery)
aiohttp>=3.9.0
